package com.gigaset.home;


import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.view.View;

public class ScreenIconView extends View {

	// ************** Constructors **************************
	
	public ScreenIconView(Context context) {
		super(context);
		
	}
	
	/**
	 * Constructor from XML layout
	 * @param context
	 * @param attrs
	 * @param defStyle
	 */
	public ScreenIconView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}

	public ScreenIconView(Context context, AttributeSet attrs) {
		super(context, attrs);


		//TypedArray ta = context.obtainStyledAttributes(attrs, R.styleable.wlasny_zestaw_atrybutow);			// podczepienie sie pod tablice atrybutow
		
		// Pobranie atrybutow z layoutu z XML'a
		/*
		width 	= (int) ta.getFloat(R.styleable.wlasny_zestaw_atrybutow_szerokosc, 40);				// 40 - wartosc defaultowa
		heigth 	= (int) ta.getFloat(R.styleable.wlasny_zestaw_atrybutow_wysokosc, 40);				// 40 - wartosc defaultowa
		dx      = (int) ta.getFloat(R.styleable.wlasny_zestaw_atrybutow_poz_x, 15);					// 15 - wartosc defaultowa
		dy      = (int) ta.getFloat(R.styleable.wlasny_zestaw_atrybutow_poz_y, 15);					// 15 - wartosc defaultowa
		*/
	}
	

	@Override
	protected void onDraw(Canvas canvas) {
		super.onDraw(canvas);
	}
}
